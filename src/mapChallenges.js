// Easy

// Square Each Number: Given an array of numbers, return an array with each number squared.
// Input: [1, 2, 3, 4]
// Expected output: [1, 4, 9, 16]
function squareNumbers (numbers) {
}

// Double the Numbers: Given an array of numbers, return an array with each number doubled.
// Input: [1, 2, 3, 4]
// Expected output: [2, 4, 6, 8]
function doubleNumbers (numbers) {
  
}

// Convert to Strings: Given an array of numbers, return an array with each number as a string.
// Input: [1, 2, 3, 4]
// Expected output: ["1", "2", "3", "4"]
function convertToStrings (numbers) {
  
}

// Capitalize Words: Given an array of words, return an array with each word capitalized.
// Input: ['apple', 'banana', 'cherry']
// Expected output: ["Apple", "Banana", "Cherry"]
function capitalizeWords (words) {
  
}

// Increment by Index: Given an array of numbers, return an array with each number incremented by its index.
// Input: [10, 20, 30, 40]
// Expected output: [10, 21, 32, 43]
function incrementByIndex (numbers) {
  
}

// Extract Property: Given an array of objects, return an array with a specific property of each object.
// Input: [{ name: 'Alice' }, { name: 'Bob' }, { name: 'Charlie' }]
// Expected output: ["Alice", "Bob", "Charlie"]
function extractProperty (users) {
  
}

// Boolean Transformation: Given an array of numbers, return an array of booleans indicating if the number is even.
// Input: [1, 2, 3, 4]
// Expected output: [false, true, false, true]
function booleanTransformation (numbers) {
  
}

// Format Dates: Given an array of date strings, return an array of date objects.
// Input: ['2024-12-18', '2024-12-19']
// Expected output: [Wed Dec 18 2024, Thu Dec 19 2024]
function formatDates (dates) {
  
}

// Add Prefix: Given an array of strings, return an array with each string prefixed with a specified word.
// Input: ['Table', 'Chair', 'Lamp']
// Expected output: ["Furniture: Table", "Furniture: Chair", "Furniture: Lamp"]
function addPrefix (items) {
  
}

// Create HTML Elements: Given an array of strings, return an array of HTML list item elements.
// Input: ['Apple', 'Banana', 'Cherry']
// Expected output: ["<li>Apple</li>", "<li>Banana</li>", "<li>Cherry</li>"]
function createHtmlElements (fruits) {
  
}



const mapChallenges = {
  squareNumbers,
  doubleNumbers,
  convertToStrings,
  capitalizeWords,
  incrementByIndex,
  extractProperty,
  booleanTransformation,
  formatDates,
  addPrefix,
  createHtmlElements,
};

export default mapChallenges;
